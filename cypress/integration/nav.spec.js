
describe('basic bank navigation', () => {
    beforeEach(function() {
            cy.visit('/');
          });

          it('it should navigate to', () =>{
            cy.wait(4000)

          })

})